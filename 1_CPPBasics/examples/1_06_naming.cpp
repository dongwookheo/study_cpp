/*!
 * 변수명, 함수명, 클래스명 등을 지을 때, 예약어를 사용하면 안된다 (keyword)
 * 이름을 지을 때, 간략하면서 충분히 의미를 표현할 수 있도록 짓는 것이 좋다 (num_apples(better) , num_of_apples(bad))
 * 관습적으로 변수명은 대문자로 쓰지 않는다... (대문자는 매크로에 사용)
 * 팀에서 정의한 컨벤션을 따르게 될 것
 * 함수의 이름이 너무 길어지는 경우, 주석을 적극 활용하자
 */
#include <cstdint>
#include <iostream>

// void main() {}  // keyword로 지정된 것으로 지을 수 없음
// 변수명 함수명 등을 숫자로 시작할 수 없다

int32_t main() {

  return 0;
}